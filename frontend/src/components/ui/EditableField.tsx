//making this now so i dont forget the pattern i have in my head

/*

I will use this for updating fields, if user is owner of project or task etc 
will be an ellipse type shape with a black boarder -- transparent body
will be used for fields across project, task, settings
e.g. editing name, project name, password name, task name, etc for all fields

*/

/* 
@@PARAMS:
{ value, onSave} // e.g. : project.name, onSave is specific context: createProject()
*/

/* export function EditableField({ value, onSave }: {value: string, onSave: (val: string) => Promise<void>}){
    
}*/