{
  "name": "forge",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run prisma\"",
    "prisma": "echo 'Prisma Studio is running: http://localhost:5555' && cd backend && npx prisma studio",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "nanoid": "^5.1.7"
  }
}
